syntax = "proto3";

enum Type {
    QUERY = 0;
    MUTATION = 1;
    // TODO: Add subscription support.
    // SUBSCRIPTION = 2;
}

message RequestSelection {
  Type type = 1;
  uint32 field = 2;
  uint64 fields = 3;
  repeated RequestSelection selections = 4 [packed=true];
}
