syntax = "proto3";

enum Type {
    QUERY = 0;
    MUTATION = 1;
    // TODO: Add subscription support.
    // SUBSCRIPTION = 2;
}

message Selection {
    uint64 fields = 1;
    map<uint32, Selection> selections = 2;
}

message Request {
  Type type = 1;
  Selection selection = 2;
}
